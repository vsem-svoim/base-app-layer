apiVersion: v1
kind: Service
metadata:
  name: platform-ui
  namespace: argocd
  labels:
    app: platform-ui-dashboard
spec:
  type: ExternalName
  externalName: platform-ui.platform-ui.svc.cluster.local
  ports:
  - port: 80
    targetPort: 8501
    protocol: TCP
    name: http