apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- base-event-coordination-config-event-schemas.yaml
- base-event-coordination-config-retry-policies.yaml
- base-event-coordination-config-saga-patterns.yaml
- base-event-coordination-config-transformation-mappings.yaml

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/part-of: base-platform
