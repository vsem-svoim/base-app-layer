apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: base-event-coordination
  annotations:
    argocd.argoproj.io/sync-wave: "5"

namespace: base-event-coordination

resources:
  - agents/
  - models/
  - orchestrators/
  - workflows/
  - configs/

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/name: base-event_coordination
    app.kubernetes.io/component: data-platform
    app.kubernetes.io/part-of: base-layer
    module.type: event_coordination
    deployment.wave: "7"
commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"
  module.description: "Event-driven architecture and saga coordination module"