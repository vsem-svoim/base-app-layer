apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Placeholder kustomization - actual resources will be added when manifests are implemented
resources: []

labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/part-of: base-platform
