apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: quality-monitoring-aws
  namespace: argocd
  labels:
    app.kubernetes.io/name: quality-monitoring
    app.kubernetes.io/part-of: base-system
    base.io/category: quality_monitoring
    base.io/provider: aws
spec:
  project: base-layer
  source:
    repoURL: https://github.com/vsem-svoim/base-app-layer.git
    path: platform-services/kustomize/base-layer/quality-monitoring/overlays/aws
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: base-quality-monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 3
