apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: data-distribution-aws
  namespace: argocd
  labels:
    app.kubernetes.io/name: data-distribution
    app.kubernetes.io/part-of: base-system
    base.io/category: data_distribution
    base.io/provider: aws
spec:
  project: base-layer
  source:
    repoURL: https://github.com/vsem-svoim/base-app-layer.git
    path: platform-services/kustomize/base-layer/data-distribution/overlays/aws
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: base-data-distribution
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 3
