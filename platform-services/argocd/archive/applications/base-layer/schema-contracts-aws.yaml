apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: schema-contracts-aws
  namespace: argocd
  labels:
    app.kubernetes.io/name: schema-contracts
    app.kubernetes.io/part-of: base-system
    base.io/category: schema_contracts
    base.io/provider: aws
spec:
  project: base-layer
  source:
    repoURL: https://github.com/vsem-svoim/base-app-layer.git
    path: platform-services/kustomize/base-layer/schema-contracts/overlays/aws
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: base-schema-contracts
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 3
