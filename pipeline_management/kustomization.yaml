apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: base-pipeline-management
  annotations:
    argocd.argoproj.io/sync-wave: "5"

namespace: base-pipeline-management

resources:
  - agents/
  - models/
  - orchestrators/
  - workflows/
  - configs/

commonLabels:
  app.kubernetes.io/name: base-pipeline-management
  app.kubernetes.io/component: data-platform
  app.kubernetes.io/part-of: base-layer
  module.type: pipeline-management
  deployment.wave: "5"

commonAnnotations:
  argocd.argoproj.io/sync-wave: "5"
  module.description: "Workflow orchestration and pipeline management module"